# 📝 TaskFlow - Vanilla JS To-Do List

A responsive, feature-rich task management application built entirely with **Vanilla JavaScript**, **HTML5**, and **CSS3**. This project demonstrates DOM manipulation, event bubbling, and dynamic UI updates without relying on external frameworks.

## 🚀 Features

* **Create Tasks:** Add new tasks dynamically using the input bar.
* **Edit Tasks:** specialized **Modal Popup** interface for editing existing tasks safely.
* **Task Management:**
* **Mark as Complete:** Checkbox functionality with visual strikethrough feedback.
* **Delete:** Remove tasks instantly from the DOM.


* **Responsive Design:** Mobile-friendly interface that adapts to different screen sizes (CSS Media Queries).
* **Smart Layout:** Fixed header with a scrollable list container (overflow handling).

## 🛠️ Tech Stack

* **Frontend:** HTML5, CSS3
* **Logic:** JavaScript (ES6+)
* **Concepts Used:**
* DOM Manipulation (`createElement`, `appendChild`, `prepend`)
* Event Listeners (`click`, `keypress`, `change`)
* CSS Flexbox & Positioning (`fixed`, `absolute`)
* CSS Transforms (for modal centering)



## 📸 Screenshots

*(You can upload the screenshot you showed me earlier here. To do that on GitHub, drag and drop your image into the Readme editor)*

## 📂 Project Structure

```bash
├── index.html      # Main HTML structure
├── style.css       # Styling and Responsive Design
└── script.js       # Core logic (Add, Edit, Delete, DOM manipulation)

```

## 🔧 How to Run

1. Clone the repository:
```bash
git clone https://github.com/yourusername/todo-list-app.git

```


2. Navigate to the project folder.
3. Open `index.html` in any modern web browser (Chrome, Firefox, Edge).

## 🔮 Future Improvements

* **Local Storage:** Implementing browser storage to persist tasks after refresh.
* **Search Functionality:** filtering tasks based on keywords.
* **Dark Mode:** Adding a toggle for visual themes.

---

### 💡 GitHub Sidebar "About" Description

*If you need a short sentence for the right-hand sidebar on GitHub:*

> "A responsive To-Do List application built with Vanilla JavaScript, featuring CRUD operations, modal editing, and mobile-friendly styling."
